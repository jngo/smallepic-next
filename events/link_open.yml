name: link_open
version: 1
description: User opens an external link.
required_props: [id, url]
props:
  id:
    type: string
  url:
    type: string
context: [user_id, session_id, page]
notes: >
  Captures outbound link clicks across the site.
